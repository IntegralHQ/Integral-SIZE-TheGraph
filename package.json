{
  "name": "integral-size",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ IntegralHQ/integral-size",
    "create-local": "graph create --node http://localhost:8020/ IntegralHQ/integral-size",
    "remove-local": "graph remove --node http://localhost:8020/ IntegralHQ/integral-size",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 IntegralHQ/integral-size"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
